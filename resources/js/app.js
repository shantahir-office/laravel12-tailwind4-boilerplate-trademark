import '../css/app.css';

// ✅ Import only the components you need
import {
  Collapse,
  Dropdown,
  Modal,
  Tab,
  Carousel,
  initTWE,
} from "tw-elements";

// Initialize
initTWE({ Collapse, Dropdown, Modal, Tab, Carousel });
